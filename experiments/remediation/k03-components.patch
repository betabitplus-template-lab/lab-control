--- a/repo-shell/quality-hooks/template/.pre-commit-config.yaml
+++ b/repo-shell/quality-hooks/template/.pre-commit-config.yaml
@@ -79,14 +79,6 @@
         files: (pyproject\.toml|uv\.lock)$
         stages: [pre-commit]
 
-      - id: py-lib-template-check
-        name: py-lib template update check
-        entry: uv run py-lib-template-check --quiet
-        language: system
-        pass_filenames: false
-        always_run: true
-        stages: [pre-push]
-
       - id: uv-sync-post-checkout
         name: uv sync after checkout (if deps changed)
         entry: bash -c 'if git diff --name-only "$PRE_COMMIT_FROM_REF" "$PRE_COMMIT_TO_REF" 2>/dev/null | grep -qE "pyproject.toml|uv.lock"; then uv sync --group dev --frozen; fi'
--- a/python/pyproject/template/pyproject.toml
+++ b/python/pyproject/template/pyproject.toml
@@ -58,7 +58,7 @@
   "pre-commit>=4.6.0",
   # Shared repo tooling and test support
   "py-lib-tooling @ git+[[[ tooling_git_url ]]]@[[[ tooling_git_ref ]]]#subdirectory=[[[ tooling_git_subdirectory ]]]",
-  "pyright>=1.1.410",
+  "pyright>=1.1.411",
   # Test runner and plugins
   "pytest>=9.1.0",
   "pytest-cov>=7.1.0",
